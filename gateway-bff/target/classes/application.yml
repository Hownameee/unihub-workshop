server:
  port: 8081

spring:
  application:
    name: gateway-bff

  threads:
    virtual:
      enabled: true

  cloud:
    gateway:
      routes:
        - id: backend-service
          uri: http://app-backend:4000
          predicates:
            - Path=/api/**
